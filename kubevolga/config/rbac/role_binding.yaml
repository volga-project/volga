apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kubevolga-operator-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kubevolga-operator-role
subjects:
  - kind: ServiceAccount
    name: kubevolga-operator
    namespace: kubevolga-system
